"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const idempotencyKeys_js_1 = require("./idempotencyKeys.js");
(0, vitest_1.describe)("idempotencyKeys metadata retention", () => {
    (0, vitest_1.it)("retains key/scope options for every key created in a run, even beyond 1000", async () => {
        const count = 3000;
        const keys = [];
        for (let i = 0; i < count; i++) {
            const key = await (0, idempotencyKeys_js_1.createIdempotencyKey)(`item-${i}`, { scope: "global" });
            keys.push(key);
        }
        // The very first key created should still resolve its original options.
        // With a fixed-size LRU catalog (cap 1000), the earliest ~2000 keys are
        // silently evicted and this returns undefined.
        const firstOptions = (0, idempotencyKeys_js_1.getIdempotencyKeyOptions)(keys[0]);
        (0, vitest_1.expect)(firstOptions).toEqual({ key: "item-0", scope: "global" });
        // Every key should resolve to its own original options.
        for (let i = 0; i < count; i++) {
            const options = (0, idempotencyKeys_js_1.getIdempotencyKeyOptions)(keys[i]);
            (0, vitest_1.expect)(options, `options missing for key index ${i}`).toEqual({
                key: `item-${i}`,
                scope: "global",
            });
        }
    });
    (0, vitest_1.it)("forgets options after the catalog is reset at a run boundary", async () => {
        const key = await (0, idempotencyKeys_js_1.createIdempotencyKey)("boundary-key", { scope: "global" });
        (0, vitest_1.expect)((0, idempotencyKeys_js_1.getIdempotencyKeyOptions)(key)).toEqual({ key: "boundary-key", scope: "global" });
        (0, idempotencyKeys_js_1.resetIdempotencyKeyCatalog)();
        (0, vitest_1.expect)((0, idempotencyKeys_js_1.getIdempotencyKeyOptions)(key)).toBeUndefined();
    });
});
//# sourceMappingURL=idempotencyKeys.test.js.map