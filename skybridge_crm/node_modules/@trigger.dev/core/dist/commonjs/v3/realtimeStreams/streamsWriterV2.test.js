"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const errors_js_1 = require("../errors.js");
const streamsWriterV2_js_1 = require("./streamsWriterV2.js");
// The size cap and discriminant extraction are the only S2-independent bits
// of `StreamsWriterV2` that benefit from unit coverage. Both live in the
// `encodeChunkOrError` pure helper, so the tests exercise it directly — no
// `vi.mock("@s2-dev/streamstore", ...)` shim needed.
(0, vitest_1.describe)("encodeChunkOrError", () => {
    (0, vitest_1.it)("flags oversize chunks and carries the chunk's `type` discriminant", () => {
        const oversized = {
            type: "tool-output-available",
            output: { text: "x".repeat(2_000_000) },
        };
        const result = (0, streamsWriterV2_js_1.encodeChunkOrError)(oversized);
        (0, vitest_1.expect)(result.ok).toBe(false);
        if (result.ok)
            return; // type guard
        (0, vitest_1.expect)((0, errors_js_1.isChatChunkTooLargeError)(result.error)).toBe(true);
        (0, vitest_1.expect)(result.error.chunkType).toBe("tool-output-available");
        (0, vitest_1.expect)(result.error.chunkSize).toBeGreaterThan(1_000_000);
        (0, vitest_1.expect)(result.error.maxSize).toBe(1024 * 1024 - 1024);
        (0, vitest_1.expect)(result.error.message).toMatch(/tool-output-available/);
        (0, vitest_1.expect)(result.error.message).toMatch(/chat\.agent chunk/);
    });
    (0, vitest_1.it)("falls back to chunk.kind when chunk.type is missing (ChatInputChunk-style)", () => {
        const oversized = { kind: "action", payload: "x".repeat(2_000_000) };
        const result = (0, streamsWriterV2_js_1.encodeChunkOrError)(oversized);
        (0, vitest_1.expect)(result.ok).toBe(false);
        if (result.ok)
            return;
        (0, vitest_1.expect)(result.error.chunkType).toBe("action");
    });
    (0, vitest_1.it)("omits chunkType when the chunk has no discriminant", () => {
        const oversized = "x".repeat(2_000_000);
        const result = (0, streamsWriterV2_js_1.encodeChunkOrError)(oversized);
        (0, vitest_1.expect)(result.ok).toBe(false);
        if (result.ok)
            return;
        (0, vitest_1.expect)(result.error.chunkType).toBeUndefined();
    });
    (0, vitest_1.it)("returns the encoded body for chunks under the cap", () => {
        const small = { type: "text-delta", delta: "hello" };
        const result = (0, streamsWriterV2_js_1.encodeChunkOrError)(small);
        (0, vitest_1.expect)(result.ok).toBe(true);
        if (!result.ok)
            return;
        const parsed = JSON.parse(result.body);
        (0, vitest_1.expect)(parsed.data).toEqual(small);
        (0, vitest_1.expect)(parsed.id).toMatch(/^[A-Za-z0-9_-]{7}$/); // nanoid(7)
    });
});
// Cross-check the ChatChunkTooLargeError type-guard helper itself. Trivial,
// but keeps the test surface here exercising the public error helpers a
// consumer would import from the same module.
(0, vitest_1.describe)("isChatChunkTooLargeError", () => {
    (0, vitest_1.it)("recognizes its own error class", () => {
        const err = new errors_js_1.ChatChunkTooLargeError(2_000_000, 1024 * 1024 - 1024, "x");
        (0, vitest_1.expect)((0, errors_js_1.isChatChunkTooLargeError)(err)).toBe(true);
    });
    (0, vitest_1.it)("rejects unrelated errors", () => {
        (0, vitest_1.expect)((0, errors_js_1.isChatChunkTooLargeError)(new Error("nope"))).toBe(false);
        (0, vitest_1.expect)((0, errors_js_1.isChatChunkTooLargeError)("string")).toBe(false);
        (0, vitest_1.expect)((0, errors_js_1.isChatChunkTooLargeError)(undefined)).toBe(false);
    });
});
//# sourceMappingURL=streamsWriterV2.test.js.map