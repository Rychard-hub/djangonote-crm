"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const manager_js_1 = require("./manager.js");
function getCached(manager, runId, key) {
    return manager.getCachedCreateStream(runId, key);
}
function makeApiClient(impl) {
    const spy = vitest_1.vi.fn(impl);
    const client = { createStream: spy };
    return { client, spy };
}
(0, vitest_1.describe)("StandardRealtimeStreamsManager createStream cache", () => {
    (0, vitest_1.it)("dedupes repeated calls for the same (runId, key)", async () => {
        const { client, spy } = makeApiClient(async () => ({ version: "v1", headers: {} }));
        const manager = new manager_js_1.StandardRealtimeStreamsManager(client, "http://localhost");
        const p1 = getCached(manager, "run-1", "chat");
        const p2 = getCached(manager, "run-1", "chat");
        (0, vitest_1.expect)(p1).toBe(p2);
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(1);
        await Promise.all([p1, p2]);
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(1);
    });
    (0, vitest_1.it)("issues a separate PUT for each distinct stream key on the same run", async () => {
        const { client, spy } = makeApiClient(async () => ({ version: "v1", headers: {} }));
        const manager = new manager_js_1.StandardRealtimeStreamsManager(client, "http://localhost");
        await Promise.all([
            getCached(manager, "run-1", "chat"),
            getCached(manager, "run-1", "tool-output"),
        ]);
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(2);
        (0, vitest_1.expect)(spy).toHaveBeenNthCalledWith(1, "run-1", "self", "chat", undefined);
        (0, vitest_1.expect)(spy).toHaveBeenNthCalledWith(2, "run-1", "self", "tool-output", undefined);
    });
    (0, vitest_1.it)("issues a separate PUT for each distinct run, even with the same key", async () => {
        const { client, spy } = makeApiClient(async () => ({ version: "v1", headers: {} }));
        const manager = new manager_js_1.StandardRealtimeStreamsManager(client, "http://localhost");
        await Promise.all([getCached(manager, "run-1", "chat"), getCached(manager, "run-2", "chat")]);
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(2);
    });
    (0, vitest_1.it)("evicts on failure so the next call retries instead of returning a poisoned entry", async () => {
        const spy = vitest_1.vi
            .fn()
            .mockRejectedValueOnce(new Error("boom"))
            .mockResolvedValueOnce({ version: "v1", headers: {} });
        const client = { createStream: spy };
        const manager = new manager_js_1.StandardRealtimeStreamsManager(client, "http://localhost");
        await (0, vitest_1.expect)(getCached(manager, "run-1", "chat")).rejects.toThrow("boom");
        const retried = await getCached(manager, "run-1", "chat");
        (0, vitest_1.expect)(retried).toEqual({ version: "v1", headers: {} });
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(2);
    });
    (0, vitest_1.it)("reset() clears cached entries so the next call re-PUTs", async () => {
        const { client, spy } = makeApiClient(async () => ({ version: "v1", headers: {} }));
        const manager = new manager_js_1.StandardRealtimeStreamsManager(client, "http://localhost");
        await getCached(manager, "run-1", "chat");
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(1);
        manager.reset();
        await getCached(manager, "run-1", "chat");
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(2);
    });
    (0, vitest_1.it)("evictCreateStreamIfStale clears the matching entry so the next call re-PUTs", async () => {
        const { client, spy } = makeApiClient(async () => ({ version: "v1", headers: {} }));
        const manager = new manager_js_1.StandardRealtimeStreamsManager(client, "http://localhost");
        // Prime the cache and capture which promise was stored.
        const cachedPromise = getCached(manager, "run-1", "chat");
        await cachedPromise;
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(1);
        // Simulate the reactive invalidation path that `pipe()` runs when a
        // writer's `wait()` rejects.
        manager.evictCreateStreamIfStale("run-1", "chat", cachedPromise);
        await getCached(manager, "run-1", "chat");
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(2);
    });
    (0, vitest_1.it)("evictCreateStreamIfStale is a no-op when the cache holds a different promise", async () => {
        const { client, spy } = makeApiClient(async () => ({ version: "v1", headers: {} }));
        const manager = new manager_js_1.StandardRealtimeStreamsManager(client, "http://localhost");
        const original = getCached(manager, "run-1", "chat");
        await original;
        // A different promise (e.g. from a concurrent caller that already
        // refreshed) shouldn't trigger eviction.
        const stalePromise = Promise.resolve({ version: "v1", headers: {} });
        manager.evictCreateStreamIfStale("run-1", "chat", stalePromise);
        // Cache should still hold the original entry; next call is a hit.
        await getCached(manager, "run-1", "chat");
        (0, vitest_1.expect)(spy).toHaveBeenCalledTimes(1);
    });
});
//# sourceMappingURL=manager.test.js.map