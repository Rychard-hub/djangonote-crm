"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const v3_1 = require("@trigger.dev/core/v3");
const auth_js_1 = require("./auth.js");
const runs_js_1 = require("./runs.js");
const triggerClient_js_1 = require("./triggerClient.js");
function installFetchSpy() {
    const captured = [];
    const originalFetch = globalThis.fetch;
    globalThis.fetch = (async (input, init) => {
        const url = typeof input === "string" ? input : (input?.url ?? String(input));
        const headers = new Headers(init?.headers);
        captured.push({
            url,
            authorization: headers.get("authorization") ?? undefined,
            branch: headers.get("x-trigger-branch") ?? undefined,
        });
        // Return a fake successful response shaped like an empty run retrieval.
        return new Response(JSON.stringify({}), {
            status: 200,
            headers: { "content-type": "application/json" },
        });
    });
    return {
        captured,
        restore: () => {
            globalThis.fetch = originalFetch;
        },
    };
}
(0, vitest_1.describe)("TriggerClient", () => {
    let fetchSpy;
    (0, vitest_1.beforeEach)(() => {
        v3_1.apiClientManager.disable();
        fetchSpy = installFetchSpy();
    });
    (0, vitest_1.afterEach)(() => {
        fetchSpy.restore();
        v3_1.apiClientManager.disable();
        v3_1.taskContext.disable();
        vitest_1.vi.unstubAllEnvs();
    });
    (0, vitest_1.it)("throws on first API call when no accessToken is configured anywhere", () => {
        const client = new triggerClient_js_1.TriggerClient();
        (0, vitest_1.expect)(() => client.runs.list({ limit: 1 })).toThrow(/TRIGGER_SECRET_KEY/);
    });
    (0, vitest_1.it)("falls back to env vars when constructor config is empty", async () => {
        vitest_1.vi.stubEnv("TRIGGER_SECRET_KEY", "tr_dev_env_token");
        vitest_1.vi.stubEnv("TRIGGER_PREVIEW_BRANCH", "env-branch");
        const client = new triggerClient_js_1.TriggerClient();
        await client.runs.retrieve("run_abc").catch(() => undefined);
        (0, vitest_1.expect)(fetchSpy.captured).toHaveLength(1);
        (0, vitest_1.expect)(fetchSpy.captured[0].authorization).toBe("Bearer tr_dev_env_token");
        (0, vitest_1.expect)(fetchSpy.captured[0].branch).toBe("env-branch");
    });
    (0, vitest_1.it)("uses the instance accessToken and previewBranch on outgoing requests", async () => {
        const client = new triggerClient_js_1.TriggerClient({
            accessToken: "tr_preview_instance_token",
            previewBranch: "signup-flow",
        });
        await client.runs.retrieve("run_abc").catch(() => undefined);
        (0, vitest_1.expect)(fetchSpy.captured).toHaveLength(1);
        const req = fetchSpy.captured[0];
        (0, vitest_1.expect)(req.authorization).toBe("Bearer tr_preview_instance_token");
        (0, vitest_1.expect)(req.branch).toBe("signup-flow");
    });
    (0, vitest_1.it)("fills missing fields from env, but explicit constructor values still win", async () => {
        vitest_1.vi.stubEnv("TRIGGER_SECRET_KEY", "tr_env_token");
        vitest_1.vi.stubEnv("TRIGGER_PREVIEW_BRANCH", "env-branch");
        vitest_1.vi.stubEnv("TRIGGER_API_URL", "https://env.example.com");
        const explicit = new triggerClient_js_1.TriggerClient({
            accessToken: "tr_explicit",
            previewBranch: "explicit-branch",
        });
        const fromEnv = new triggerClient_js_1.TriggerClient();
        await Promise.all([
            explicit.runs.retrieve("run_a").catch(() => undefined),
            fromEnv.runs.retrieve("run_b").catch(() => undefined),
        ]);
        const byRun = Object.fromEntries(fetchSpy.captured.map((r) => [r.url.split("/runs/")[1]?.split(/[/?]/)[0], r]));
        (0, vitest_1.expect)(byRun["run_a"].authorization).toBe("Bearer tr_explicit");
        (0, vitest_1.expect)(byRun["run_a"].branch).toBe("explicit-branch");
        (0, vitest_1.expect)(byRun["run_b"].authorization).toBe("Bearer tr_env_token");
        (0, vitest_1.expect)(byRun["run_b"].branch).toBe("env-branch");
        (0, vitest_1.expect)(byRun["run_a"].url.startsWith("https://env.example.com/")).toBe(true);
    });
    (0, vitest_1.it)("does not leak instance config to the global apiClientManager", async () => {
        (0, auth_js_1.configure)({ accessToken: "tr_dev_global_token" });
        const client = new triggerClient_js_1.TriggerClient({
            accessToken: "tr_preview_instance_token",
            previewBranch: "signup-flow",
        });
        await client.runs.retrieve("run_instance").catch(() => undefined);
        await runs_js_1.runs.retrieve("run_global").catch(() => undefined);
        (0, vitest_1.expect)(fetchSpy.captured).toHaveLength(2);
        (0, vitest_1.expect)(fetchSpy.captured[0].authorization).toBe("Bearer tr_preview_instance_token");
        (0, vitest_1.expect)(fetchSpy.captured[0].branch).toBe("signup-flow");
        (0, vitest_1.expect)(fetchSpy.captured[1].authorization).toBe("Bearer tr_dev_global_token");
        (0, vitest_1.expect)(fetchSpy.captured[1].branch).toBeUndefined();
    });
    (0, vitest_1.it)("keeps two concurrent instances isolated from each other", async () => {
        const prod = new triggerClient_js_1.TriggerClient({ accessToken: "tr_prod_key" });
        const preview = new triggerClient_js_1.TriggerClient({
            accessToken: "tr_preview_key",
            previewBranch: "feature-x",
        });
        await Promise.all([
            prod.runs.retrieve("run_a").catch(() => undefined),
            preview.runs.retrieve("run_b").catch(() => undefined),
            prod.runs.retrieve("run_c").catch(() => undefined),
            preview.runs.retrieve("run_d").catch(() => undefined),
        ]);
        (0, vitest_1.expect)(fetchSpy.captured).toHaveLength(4);
        const byPath = Object.fromEntries(fetchSpy.captured.map((r) => [r.url.split("/runs/")[1]?.split(/[/?]/)[0], r]));
        (0, vitest_1.expect)(byPath["run_a"].authorization).toBe("Bearer tr_prod_key");
        (0, vitest_1.expect)(byPath["run_a"].branch).toBeUndefined();
        (0, vitest_1.expect)(byPath["run_c"].authorization).toBe("Bearer tr_prod_key");
        (0, vitest_1.expect)(byPath["run_c"].branch).toBeUndefined();
        (0, vitest_1.expect)(byPath["run_b"].authorization).toBe("Bearer tr_preview_key");
        (0, vitest_1.expect)(byPath["run_b"].branch).toBe("feature-x");
        (0, vitest_1.expect)(byPath["run_d"].authorization).toBe("Bearer tr_preview_key");
        (0, vitest_1.expect)(byPath["run_d"].branch).toBe("feature-x");
    });
    (0, vitest_1.it)("masks taskContext.ctx inside an isolated scope (default)", () => {
        const fakeCtx = {
            run: { id: "run_parent", isTest: true },
            project: { ref: "proj_xyz" },
            environment: { slug: "preview" },
        };
        v3_1.taskContext.setGlobalTaskContext({ ctx: fakeCtx });
        (0, vitest_1.expect)(v3_1.taskContext.ctx).toBe(fakeCtx);
        const observed = v3_1.sdkScope.withScope({ apiClientConfig: { accessToken: "x" }, inheritContext: false }, () => v3_1.taskContext.ctx);
        (0, vitest_1.expect)(observed).toBeUndefined();
    });
    (0, vitest_1.it)("exposes taskContext.ctx inside a scope when inheritContext is true", () => {
        const fakeCtx = {
            run: { id: "run_parent", isTest: true },
            project: { ref: "proj_xyz" },
            environment: { slug: "preview" },
        };
        v3_1.taskContext.setGlobalTaskContext({ ctx: fakeCtx });
        const observed = v3_1.sdkScope.withScope({ apiClientConfig: { accessToken: "x" }, inheritContext: true }, () => v3_1.taskContext.ctx);
        (0, vitest_1.expect)(observed).toBe(fakeCtx);
    });
});
(0, vitest_1.describe)("configure()", () => {
    (0, vitest_1.beforeEach)(() => {
        v3_1.apiClientManager.disable();
    });
    (0, vitest_1.afterEach)(() => {
        v3_1.apiClientManager.disable();
    });
    (0, vitest_1.it)("overrides previously-set configuration on a second call", async () => {
        (0, auth_js_1.configure)({ accessToken: "tr_first" });
        (0, vitest_1.expect)(v3_1.apiClientManager.accessToken).toBe("tr_first");
        (0, auth_js_1.configure)({ accessToken: "tr_second", previewBranch: "branch-b" });
        (0, vitest_1.expect)(v3_1.apiClientManager.accessToken).toBe("tr_second");
        (0, vitest_1.expect)(v3_1.apiClientManager.branchName).toBe("branch-b");
    });
});
(0, vitest_1.describe)("auth.withAuth", () => {
    (0, vitest_1.beforeEach)(() => {
        v3_1.apiClientManager.disable();
    });
    (0, vitest_1.afterEach)(() => {
        v3_1.apiClientManager.disable();
    });
    (0, vitest_1.it)("inherits TRIGGER_SECRET_KEY from env when called with a partial config", async () => {
        vitest_1.vi.stubEnv("TRIGGER_SECRET_KEY", "tr_dev_env_token");
        let observed;
        await auth_js_1.auth.withAuth({ baseURL: "https://override.example.com" }, async () => {
            observed = v3_1.apiClientManager.accessToken;
        });
        // The scoped `inheritContext: true` path falls back to TRIGGER_SECRET_KEY
        // so callers can override only baseURL without re-passing the token.
        (0, vitest_1.expect)(observed).toBe("tr_dev_env_token");
        // baseURL override still applies.
        (0, vitest_1.expect)(await auth_js_1.auth.withAuth({ baseURL: "https://override.example.com" }, async () => v3_1.apiClientManager.baseURL)).toBe("https://override.example.com");
    });
    (0, vitest_1.it)("composes nested withAuth: outer-scope fields flow into the inner scope", async () => {
        vitest_1.vi.stubEnv("TRIGGER_SECRET_KEY", "tr_env_token");
        let observedBaseURL;
        let observedAuth;
        await auth_js_1.auth.withAuth({ baseURL: "https://outer.example.com" }, async () => {
            await auth_js_1.auth.withAuth({ accessToken: "tr_inner_token" }, async () => {
                observedBaseURL = v3_1.apiClientManager.baseURL;
                observedAuth = v3_1.apiClientManager.accessToken;
            });
        });
        (0, vitest_1.expect)(observedBaseURL).toBe("https://outer.example.com");
        (0, vitest_1.expect)(observedAuth).toBe("tr_inner_token");
    });
    (0, vitest_1.it)("does not stomp on a parallel withAuth call with a different config", async () => {
        (0, auth_js_1.configure)({ accessToken: "tr_global" });
        const tokenA = "tr_concurrent_a";
        const tokenB = "tr_concurrent_b";
        const settle = {
            resolveA: () => { },
            resolveB: () => { },
        };
        const gateA = new Promise((r) => (settle.resolveA = r));
        const gateB = new Promise((r) => (settle.resolveB = r));
        const runA = auth_js_1.auth.withAuth({ accessToken: tokenA }, async () => {
            // Suspend mid-scope so the parallel B scope opens while A is still pending.
            await gateA;
            return v3_1.apiClientManager.accessToken;
        });
        const runB = auth_js_1.auth.withAuth({ accessToken: tokenB }, async () => {
            // Open B's scope first, then unblock A. If withAuth used the old
            // mutate-and-restore pattern, A would observe tokenB or B's
            // .finally would restore the wrong "original".
            const seenInB = v3_1.apiClientManager.accessToken;
            settle.resolveA();
            await gateB;
            return seenInB;
        });
        settle.resolveB(); // let B finish after A reads
        const [seenInA, seenInB] = await Promise.all([runA, runB]);
        (0, vitest_1.expect)(seenInA).toBe(tokenA);
        (0, vitest_1.expect)(seenInB).toBe(tokenB);
        // Global remains unchanged after both scopes exit.
        (0, vitest_1.expect)(v3_1.apiClientManager.accessToken).toBe("tr_global");
    });
});
//# sourceMappingURL=triggerClient.test.js.map